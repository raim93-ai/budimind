# S00-T01 Evidence — Inventory and Quarantine

Task: S00-T01 Inventory and quarantine  
Status: COMPLETE  
Observed: 2026-09-10  
Branch/commit: existing working tree; no commit created

## Files changed

- `docs/current-state.md`
- `docs/legacy-salvage-register.md`
- `docs/evidence/S00-T01.md`

## Commands run

```text
git status --short
git -C psychology-clinic-context status --short
rg --files -g '!node_modules/**' -g '!tmp/**'
Get-ChildItem apps,packages -Recurse -Filter package.json
Get-ChildItem python,apps -Recurse -File | Where-Object {$_.Extension -in '.py','.toml'}
```

## Results

- Root repository is an uncommitted scaffold; all existing work remains untouched.
- Nested context repository has pre-existing modified documents and untracked planning documents; no reset/checkout was
  performed.
- Legacy review clone is ignored under `tmp/` and is not imported by any source path found in the inventory.
- Two web apps, two API namespaces, one worker, shared UI/contracts/config packages, local Compose, and empty intended
  Python/infra/test directories were recorded.
- No Python project manifest, migration directory, domain implementation, or application authentication implementation
  was found.
- Existing legacy patterns that violate the product boundary were explicitly marked rejected in the salvage register.
- The root `nul` artifact was recorded as unresolved and not deleted.

## Security/privacy checks

- No secrets, credentials, tokens, clinical content, corporate responses, or real-person data were added.
- No runtime code, dependency, schema, infrastructure, or deletion change was made.
- Legacy runtime import/quarantine rule is documented.

## UI evidence

N/A. UI changes are prohibited in S00-T01.

## Known limitations

- Inventory is structural; runtime behavior, dependency installation, and build health are intentionally deferred to
  S00-T02 through S00-T04.
- The current working tree has no commit baseline yet.

## Human decisions required

- Confirm whether the root `nul` file should be removed in a later task.
- Review owners/licences for any item proposed from the legacy register.

Next permitted task: S00-T02 Pin the toolchain
