# S00-T02 Evidence — Pin the Toolchain

Task: S00-T02 Pin the toolchain  
Status: PARTIAL  
Observed: 2026-09-10  
Branch/commit: existing working tree; no commit created

## Files changed

- `package.json`, `pnpm-lock.yaml`, `.prettierrc`, `.prettierignore`
- `apps/corporate-web/package.json`, `apps/clinic-web/package.json`, `packages/ui/package.json`
- `apps/corporate-web/next.config.js`, `apps/clinic-web/next.config.js`
- `eslint.config.mjs`, `tsconfig.json`
- `pyproject.toml`, `uv.lock`, `apps/**/__init__.py`
- `.github/dependabot.yml`

## Results

- Web applications and UI package target Next.js 16.3.4, React 19.2.0, and React DOM 19.2.0.
- Root engine targets Node 24 and package manager metadata targets pnpm 10.15.1.
- Python 3.13 is locked in `uv.lock`; FastAPI, SQLAlchemy, Alembic, logging, test, lint, and typing dependencies are
  resolved.
- Deprecated Next.js `eslint` config and obsolete static cache header were removed.
- Dependabot policy covers npm, Python, and Docker updates.
- Current environment executed pnpm 9.15.9 because Corepack could not write its machine cache. The lockfile is valid,
  but pnpm 10 activation remains to be verified on a clean developer/CI host.

## Commands run

```text
pnpm install --lockfile-only --ignore-scripts --config.confirmModulesPurge=false
pnpm install --frozen-lockfile --ignore-scripts --config.confirmModulesPurge=false
uv lock --python 3.13
uv sync --locked
```

## Known limitations

- This task is partial until a clean host verifies the pinned pnpm 10 runtime and CI uses it.
- Application dependencies are pinned, but domain behavior remains scaffold work in later stages.

Next permitted task: S00-T03 Make local infrastructure real
