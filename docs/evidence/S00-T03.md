# S00-T03 Evidence — Local Infrastructure

Task: S00-T03 Make local infrastructure real  
Status: BLOCKED  
Observed: 2026-09-10

## Files changed

- `compose.yaml` — removed unused Redis/LocalStack services, removed obsolete Compose version field, upgraded PostgreSQL
  images to 17, retained separate corporate/clinical databases and ports.

## Commands run

```text
docker compose config
docker compose up -d
```

## Results

- `docker compose config` passed and shows only separate `corporate-db` and `clinical-db` services on ports 5432/5433.
- `docker compose up -d` could not start because Docker Desktop's Linux engine named pipe is unavailable:
  `The system cannot find the file specified.`

## Security/privacy checks

- No real data, credentials, or tokens were added.
- Corporate and clinical database names, ports, and volumes remain distinct.
- Startup, health, migration, and database credential isolation are unverified until Docker is running.

## Human/environment action required

Start Docker Desktop or provide a reachable Docker daemon, then rerun this task before Stage 00 can close.

Next permitted task after unblock: S00-T04 Make the root command contract real
