# S00-T03 Evidence — Local Infrastructure

Task: S00-T03 Make local infrastructure real  
Status: BLOCKED  
Observed: 2026-09-11

## Files changed

- `compose.yaml` — keeps only two pinned MySQL 8.4 services with separate databases, credentials, volumes, ports and
  internal Docker networks.

## Commands run

```text
docker compose config
docker compose up -d
```

## Results

- `docker compose config` passed on 2026-09-11 and resolves only the separate `corporate-db` and `clinical-db` MySQL
  services on host ports 3306/3307, each with its own internal Docker network, credentials and volume.
- `docker compose up -d` could not start because Docker Desktop's Linux engine named pipe is unavailable:
  `open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.`
- Starting Docker Desktop and restarting its WSL backend were attempted, but this environment could not obtain the
  required Windows service access. No container result is claimed.

## Security/privacy checks

- No real data, credentials, or tokens were added.
- Corporate and clinical database names, users, ports, volumes and internal networks are distinct.
- Startup, health, migration, and database credential isolation are unverified until Docker is running.

## Human/environment action required

Start Docker Desktop or provide a reachable Docker daemon, then rerun this task before Stage 00 can close.

Next permitted task after unblock: S00-T03 Make local infrastructure real
