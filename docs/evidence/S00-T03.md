# S00-T03 Evidence — Local Infrastructure

Task: S00-T03 Make local infrastructure real  
Status: COMPLETE
Observed: 2026-09-11

## Files changed

- `compose.yaml` — keeps only two pinned MySQL 8.4 services with separate databases, credentials, volumes, ports and
  internal Docker networks.

## Commands run

```text
docker compose config
docker compose up -d
```

## Results

- `docker compose config` passed on 2026-09-11 and resolves only the separate `corporate-db` and `clinical-db` MySQL
  services on host ports 3306/3307, each with its own internal Docker network, credentials and volume.
- `docker info` reported server version `29.7.2`.
- `docker compose up -d --force-recreate` started both services; both report `healthy`.
- Read-only checks confirmed `corporate_app` can see only `budimind_corporate` (plus system schemas), and `clinical_app`
  can see only `budimind_clinical` (plus system schemas).
- Baseline migration `0001_baseline` was applied and verified in both databases using the MySQL client inside each
  container. The host-side migration command remains the deployment path; local Windows host networking is restricted in
  this environment, so the container-internal check is the authoritative local proof.

## Security/privacy checks

- No real data, credentials, or tokens were added.
- Corporate and clinical database names, users, ports, volumes and internal networks are distinct.
- Startup, health, migration marker, and database credential isolation are verified for the local scaffold.

## Human/environment action required

No environment action is required for S00-T03. Proceed to S00-T04 command-target completion.
