# S02-T01 — Trusted platform database foundation

Status: COMPLETE FOR SYNTHETIC IMPLEMENTATION

Implemented independently in both Supabase migration roots:

- `private.app_actor` with explicit plane-local role allowlists and lifecycle status.
- `private.server_session` storing only token hashes, with expiry, rotation, revocation, and actor foreign key.
- `private.idempotency_key` keyed by actor/action/request hash, with no raw request token storage.
- `private.audit_event` with purpose, outcome, correlation, and append-only update/delete trigger.
- `private.outbox_job` for durable, retryable side effects; no cross-plane foreign keys.

Verification:

- `uv run python scripts/reset_test_db.py` followed by `uv run python scripts/migrate.py` completed against both Docker
  PostgreSQL services.
- Both databases report two migration markers and all five private tables.
- A clinical audit update was rejected with `audit_event is append-only`.
- `git diff --check` passed.

Limit: hosted Supabase projects and service-role grants require credentials and are intentionally deferred to
deployment. No real-person data or external side effects are enabled.
