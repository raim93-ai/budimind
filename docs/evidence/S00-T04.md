# S00-T04 Evidence — Root Command Contract

Task: S00-T04 Make the root command contract real  
Status: PARTIAL  
Observed: 2026-09-11

## Files changed

- Root package scripts and formatter configuration.
- Python scaffold formatting, annotations, package markers, and minimal boundary regression test.

## Passing commands

```text
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test                 # 2 files, 2 tests passed
pnpm build                # both Next.js apps and both TypeScript packages passed
pnpm run python:lint      # ruff passed
pnpm run python:typecheck # all API/worker files passed
pnpm run python:test      # 1 test passed
```

The JavaScript build was rerun after the pre-launch clinic copy/UI remediation: both Next.js applications and both
TypeScript packages passed. Python lint and three strict mypy scopes passed; pytest passed one boundary test. Vitest
passed two files/two tests.

## Remaining failures/gaps

- `pnpm test:integration` has no `vitest.integration.config.ts` yet.
- `pnpm test:e2e` has no Playwright configuration/specs yet.
- `pnpm db:migrate` and `pnpm db:reset:test` now have guarded SQLAlchemy/asyncpg implementations in `scripts/`.
- Docker-dependent health/migration checks are complete for the local scaffold (see S00-T03); the host-side command is
  still environment-dependent because this Windows sandbox cannot reach published Docker ports.

No command was silenced, skipped, or marked successful without output. Stage 00 remains open pending integration and
end-to-end test harnesses.

Next permitted task: finish S00-T02/S00-T03, then implement the missing command targets before S00-T04 can be complete.
