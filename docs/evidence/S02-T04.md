# S02-T04 — Web shells and shared primitives

Status: COMPLETE FOR SYNTHETIC IMPLEMENTATION

Implemented:

- Shared `ServiceState` primitive for maintenance and expired-session routes.
- `/maintenance` and `/session-expired` routes in both web shells.
- Corporate shell no longer uses automatic dark-mode variables or a gradient hero.
- Removed fabricated corporate trust signals and replaced unsupported launch copy with truthful pre-launch language.
- Added responsive/reduced-motion/no-horizontal-overflow checks at 390, 768 and 1440 pixels for both shells.

Verification:

- `corepack pnpm format:check` — passed.
- `corepack pnpm lint` — passed.
- `corepack pnpm typecheck` — passed.
- `corepack pnpm test` — 2 package tests passed.
- `corepack pnpm test:integration` — 2 contract tests passed.
- `corepack pnpm python:lint` — passed.
- `corepack pnpm python:typecheck` — all API/worker files passed.
- `corepack pnpm python:test` — 19 tests passed.
- `corepack pnpm db:reset:test` and `corepack pnpm db:migrate` — passed.
- `corepack pnpm build` — both web apps and shared packages built successfully.
- `corepack pnpm test:e2e` — 4 tests passed across clinic and corporate shells.
- `git diff --check` — passed.

Known limit: a dedicated axe audit and production-domain visual review remain part of Stage 10 hardening; no production
or real-person content is enabled.
