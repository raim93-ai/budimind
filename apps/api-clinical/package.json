{
  "name": "api-clinical",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --port 8001",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8001",
    "lint": "ruff check .",
    "format": "ruff format .",
    "typecheck": "mypy .",
    "test": "pytest"
  }
}
